<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues/>
  <CurrentIssues>
    <ID>InjectDispatcher:MainViewModel.kt$MainViewModel$IO</ID>
    <ID>InjectDispatcher:SettingsViewModel.kt$SettingsViewModel$IO</ID>
    <ID>InjectDispatcher:SyncWorker.kt$SyncWorker$IO</ID>
    <ID>LargeClass:SettingsViewModel.kt$SettingsViewModel : AndroidViewModel</ID>
    <ID>LongParameterList:ImportSettingsScreen.kt$( isServerConfigured: Boolean, isScanning: Boolean, isImporting: Boolean, showScanResults: Boolean, scanResults: List&lt;NotesImportWizard.ImportCandidate&gt;, selectedCandidates: Set&lt;Int&gt;, onScanClick: () -&gt; Unit, onSelectionChange: (Int, Boolean) -&gt; Unit, onCancelScan: () -&gt; Unit, onImportSelected: () -&gt; Unit, )</ID>
    <ID>RedundantSuspendModifier:ParallelDownloader.kt$ParallelDownloader$suspend</ID>
    <ID>UnnecessarySafeCall:AboutScreen.kt$appIcon?.let { Image( bitmap = it, contentDescription = stringResource(R.string.about_app_name), modifier = Modifier.size(96.dp) ) }</ID>
    <ID>UnnecessarySafeCall:EmptyState.kt$appIcon?.let { Image( bitmap = it, contentDescription = null, modifier = Modifier.size(96.dp) ) }</ID>
    <ID>UnreachableCode:NetworkMonitor.kt$NetworkMonitor$capabilities.hasTransport(NetworkCapabilities.TRANSPORT_WIFI)</ID>
    <ID>UnreachableCode:NetworkMonitor.kt$NetworkMonitor$val activeNetwork = connectivityManager.activeNetwork ?: return false</ID>
    <ID>UnreachableCode:NetworkMonitor.kt$NetworkMonitor$val capabilities = connectivityManager.getNetworkCapabilities(activeNetwork) ?: return false</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$Logger.d(TAG, "Toggled + auto-sorted checklist item '$itemId' in widget")</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$NoteWidget().update(context, glanceId)</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$storage.saveNote(updatedNote)</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$updateAppWidgetState(context, glanceId) { prefs -&gt; prefs[NoteWidgetState.KEY_LAST_UPDATED] = System.currentTimeMillis() }</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$val sortOption = try { note.checklistSortOption?.let { ChecklistSortOption.valueOf(it) } } catch (@Suppress("SwallowedException") e: IllegalArgumentException) { null } ?: ChecklistSortOption.MANUAL</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$val sortedItems = if (sortOption == ChecklistSortOption.MANUAL || sortOption == ChecklistSortOption.UNCHECKED_FIRST) { val unchecked = updatedItems.filter { !it.isChecked }.sortedBy { it.originalOrder } val checked = updatedItems.filter { it.isChecked }.sortedBy { it.originalOrder } (unchecked + checked).mapIndexed { index, item -&gt; item.copy(order = index) } } else { updatedItems.mapIndexed { index, item -&gt; item.copy(order = index) } }</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$val updatedItems = note.checklistItems?.map { item -&gt; if (item.id == itemId) { item.copy(isChecked = !item.isChecked) } else item }?.run { // 🆕 v1.9.0 (F04): Backward compat — old notes (pre-F04) have all originalOrder == 0 // (Gson default). Only patch if ALL items are 0, meaning F04 was never active for this note. // If at least one item has originalOrder != 0, the note was saved with F04 → don't touch. val isPreF04Note = all { it.originalOrder == 0 } if (isPreF04Note) map { it.copy(originalOrder = it.order) } else this } ?: return</ID>
    <ID>UnreachableCode:NoteWidgetActions.kt$ToggleChecklistItemAction$val updatedNote = note.copy( checklistItems = sortedItems, updatedAt = System.currentTimeMillis(), syncStatus = SyncStatus.PENDING )</ID>
    <ID>UnreachableCode:NoteWidgetContent.kt$Spacer(modifier = GlanceModifier.width(8.dp))</ID>
    <ID>UnreachableCode:NoteWidgetContent.kt$Text( text = if (item.isChecked) "☑️" else "☐", // 🆕 v1.8.1 (IMPL_06) style = TextStyle(fontSize = 16.sp) )</ID>
    <ID>UnreachableCode:NoteWidgetContent.kt$Text( text = item.text, style = TextStyle( color = if (item.isChecked) GlanceTheme.colors.outline else GlanceTheme.colors.onSurface, fontSize = 14.sp, textDecoration = if (item.isChecked) TextDecoration.LineThrough else TextDecoration.None ), maxLines = 2 )</ID>
    <ID>UnreachableCode:NoteWidgetContent.kt$if (isLocked) { Row( modifier = GlanceModifier .fillMaxWidth() .padding(vertical = 4.dp), // 🆕 v1.8.2 (IMPL_12): 2dp → 4dp verticalAlignment = Alignment.CenterVertically ) { Text( text = if (item.isChecked) "☑️" else "☐", // 🆕 v1.8.1 (IMPL_06) style = TextStyle(fontSize = 16.sp) ) Spacer(modifier = GlanceModifier.width(8.dp)) // 🆕 v1.9.0 (F03): Strikethrough + dimmed color for completed items Text( text = item.text, style = TextStyle( color = if (item.isChecked) GlanceTheme.colors.outline else GlanceTheme.colors.onSurface, fontSize = 14.sp, textDecoration = if (item.isChecked) TextDecoration.LineThrough else TextDecoration.None ), maxLines = 2 ) } } else { CheckBox( checked = item.isChecked, onCheckedChange = actionRunCallback&lt;ToggleChecklistItemAction&gt;( actionParametersOf( NoteWidgetActionKeys.KEY_NOTE_ID to note.id, NoteWidgetActionKeys.KEY_ITEM_ID to item.id, NoteWidgetActionKeys.KEY_GLANCE_ID to glanceId.toString() ) ), text = item.text, // 🆕 v1.9.0 (F03): Strikethrough + dimmed color for completed items style = TextStyle( color = if (item.isChecked) GlanceTheme.colors.outline else GlanceTheme.colors.onSurface, fontSize = 14.sp, textDecoration = if (item.isChecked) TextDecoration.LineThrough else TextDecoration.None ), modifier = GlanceModifier .fillMaxWidth() .padding(vertical = 3.dp) // 🆕 v1.8.2 (IMPL_12): 1dp → 3dp ) }</ID>
    <ID>UnreachableCode:NoteWidgetContent.kt$val item = items.getOrNull(itemIndex) ?: return@items</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$!syncService.hasUnsyncedChanges()</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (!gateResult.canSync) { if (gateResult.isBlockedByWifiOnly) { Logger.d(TAG, "⏭️ WiFi-only mode enabled, but not on WiFi - skipping sync") } else { Logger.d(TAG, "⏭️ Sync blocked by gate: ${gateResult.blockReason ?: "offline/no server"}") } // 🛡️ v1.8.2 (IMPL_14): State reset — tryStartSync() wurde bereits aufgerufen SyncStateManager.reset() if (BuildConfig.DEBUG) { Logger.d(TAG, "✅ SyncWorker.doWork() SUCCESS (gate blocked)") Logger.d(TAG, "═══════════════════════════════════════") } return@withContext Result.success() }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (!syncService.isServerReachable()) { Logger.d(TAG, "⏭️ Server not reachable - skipping sync (no error)") Logger.d(TAG, " Reason: Server offline/wrong network/network not ready/not configured") Logger.d(TAG, " This is normal in foreign WiFi or during network initialization") // 🔥 v1.1.2: Check if we should show warning (server unreachable for &gt;24h) checkAndShowSyncWarning(syncService) // 🛡️ v1.8.2 (IMPL_14): State reset — tryStartSync() wurde bereits aufgerufen SyncStateManager.reset() if (BuildConfig.DEBUG) { Logger.d(TAG, "✅ SyncWorker.doWork() SUCCESS (silent skip)") Logger.d(TAG, "═══════════════════════════════════════") } // Success zurückgeben (kein Fehler, Server ist halt nicht erreichbar) return@withContext Result.success() }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 4: Checking sync gate (canSync)") }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 5: Checking server reachability (Pre-Check)") }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 6: Server reachable - proceeding with sync") Logger.d(TAG, " SyncService: $syncService") }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 7: Processing result") Logger.d( TAG, "📦 Sync result: success=${result.isSuccess}, " + "count=${result.syncedCount}, error=${result.errorMessage}" ) }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$if (result.isSuccess) { if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 8: Success path") } Logger.i(TAG, "✅ Sync successful: ${result.syncedCount} notes") // 🆕 v1.8.1 (IMPL_08): SyncStateManager aktualisieren SyncStateManager.markCompleted() // Nur Notification zeigen wenn tatsächlich etwas gesynct wurde // UND die App nicht im Vordergrund ist (sonst sieht User die Änderungen direkt) if (result.syncedCount &gt; 0) { val appInForeground = isAppInForeground() if (appInForeground) { Logger.d(TAG, "ℹ️ App in foreground - skipping notification (UI shows changes)") } else { if (BuildConfig.DEBUG) { Logger.d(TAG, " Showing success notification...") } NotificationHelper.showSyncSuccess( applicationContext, result.syncedCount ) } } else { Logger.d(TAG, "ℹ️ No changes to sync - no notification") } // **UI REFRESH**: SyncEventBus für ComposeMainActivity if (BuildConfig.DEBUG) { Logger.d(TAG, " Broadcasting sync completed...") } SyncEventBus.emit(SyncEvent.SyncCompleted(success = true, count = result.syncedCount)) // 🆕 v1.8.0: Alle Widgets aktualisieren nach Sync try { if (BuildConfig.DEBUG) { Logger.d(TAG, " Updating widgets...") } val glanceManager = androidx.glance.appwidget.GlanceAppWidgetManager(applicationContext) val glanceIds = glanceManager.getGlanceIds(dev.dettmer.simplenotes.widget.NoteWidget::class.java) glanceIds.forEach { id -&gt; dev.dettmer.simplenotes.widget.NoteWidget().update(applicationContext, id) } } catch (e: Exception) { Logger.w(TAG, "Failed to update widgets: ${e.message}") } if (BuildConfig.DEBUG) { Logger.d(TAG, "✅ SyncWorker.doWork() SUCCESS") Logger.d(TAG, "═══════════════════════════════════════") } Result.success() } else { if (BuildConfig.DEBUG) { Logger.d(TAG, "📍 Step 8: Failure path") } Logger.e(TAG, "❌ Sync failed: ${result.errorMessage}") // 🆕 v1.8.1 (IMPL_08): SyncStateManager aktualisieren SyncStateManager.markError(result.errorMessage) NotificationHelper.showSyncError( applicationContext, result.errorMessage ?: "Unbekannter Fehler" ) // Notify UI auch bei Fehler SyncEventBus.emit(SyncEvent.SyncCompleted(success = false, count = 0)) if (BuildConfig.DEBUG) { Logger.d(TAG, "❌ SyncWorker.doWork() FAILURE") Logger.d(TAG, "═══════════════════════════════════════") } Result.failure() }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$val gateResult = syncService.canSync()</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker$val result = try { if (BuildConfig.DEBUG) { Logger.d(TAG, " Calling syncService.syncNotes()...") } syncService.syncNotes().also { if (BuildConfig.DEBUG) { Logger.d(TAG, " ✅ syncNotes() returned") } } } catch (e: Exception) { Logger.e(TAG, "💥 CRASH in syncNotes()!", e) Logger.e(TAG, "Exception: ${e.javaClass.name}: ${e.message}") throw e }</ID>
    <ID>UnreachableCode:SyncWorker.kt$SyncWorker${ Logger.d(TAG, "⏭️ No local changes - skipping sync (performance optimization)") Logger.d(TAG, " Saves battery, network traffic, and server load") // 🛡️ v1.8.2 (IMPL_14): State reset — tryStartSync() wurde bereits aufgerufen SyncStateManager.reset() if (BuildConfig.DEBUG) { Logger.d(TAG, "✅ SyncWorker.doWork() SUCCESS (no changes to sync)") Logger.d(TAG, "═══════════════════════════════════════") } return@withContext Result.success() }</ID>
    <ID>UnusedImports:ImportSettingsScreen.kt$import androidx.compose.foundation.lazy.items</ID>
  </CurrentIssues>
</SmellBaseline>
